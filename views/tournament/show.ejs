<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head.ejs')  %>
</head>
<body>
    <%- include('../partials/nav.ejs')  %>
    <% if(currentUser.email == tournament.organizerEmail) { %>
		<form action="/tournament/<%=tournament._id %>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE" />
          </form>
		<a href="/tournament/<%=tournament._id; %>/edit">Edit</a>
	<% } %>
    <h1><%=tournament.name %></h1>
    <h2>Contact Info:</h2>
    <p><%=tournament.organizerName %></p>
    <p><%=tournament.organizerEmail %></p>
    <h2>Start Date (yyyy/mm/dd):</h2>
    <p><%=tournament.startDate %> to <%=tournament.endDate %></p>
    <h2>Time:</h2>
	<p><%=tournament.time %></p>
    <h2>Address:</h2>
    <p><%=tournament.address %></p>
    <h2>Rules:</h2>
    <p><%=tournament.rules %></p>
    <h2>Open to Public:</h2>
    <% if(tournament.openToPublic) { %>
		<p>Yes</p>
	<% } else { %>
		<p>Invitation Only</p>
	<% } %>
    <h2>Participants:</h2>
    <p><%=tournament.numberOfParticipants %></p>
    <p><%=tournament.participants %></p>
</body>
</html>